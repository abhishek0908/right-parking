# Supabase Configuration
# Get these values from your Supabase project settings -> API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Cloudinary Configuration
# Get these values from your Cloudinary dashboard
# 1. Go to https://cloudinary.com/console
# 2. Create an upload preset (Settings -> Upload -> Upload presets -> Add upload preset)
#    - Set "Signing Mode" to "Unsigned" for client-side uploads (needed for basic uploads)
#    - Save and copy the preset name
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
VITE_CLOUDINARY_UPLOAD_PRESET=right-parking

# Cloudinary Admin API Keys (REQUIRED for secure deletions and project-wise folder organization)
# 1. Go to Cloudinary Dashboard
# 2. Copy "API Key" and "API Secret"
VITE_CLOUDINARY_API_KEY=your-api-key
VITE_CLOUDINARY_API_SECRET=your-api-secret

# (Optional) Cloudinary Base URL - only change if using a custom CNAME or different cloud
# VITE_CLOUDINARY_BASE_URL=https://res.cloudinary.com/your-cloud-name/image/upload
